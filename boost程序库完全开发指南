bjam --toolset=msvc-12.0 runtime-link=static architecture=x86 address-model=64 --with-thread --with-date_time

bjam --toolset=msvc-11.0 runtime-link=static architecture=x86 address-model=64 --with-date_time --with-thread

工作集选择2012(vc110)

(ok) 第三章:内存管理
		
		pool_allocator
			stl容器的分配器

	(ok) 第四章:实用工具
		

	第五章:字符串与文本处理
		207
